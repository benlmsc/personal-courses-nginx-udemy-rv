version: "3"

services:
  nginx:
    image: nginx:latest
    container_name: udemy-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./sites:/sites